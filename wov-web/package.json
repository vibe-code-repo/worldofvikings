{
  "name": "wov-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "world-of-vikings.com — SvelteKit; Seiten vorgerendert, Forum serverseitig gerendert",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "start": "node build",
    "vorschau": "vite build && node build",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "lint": "biome check .",
    "format": "biome check --write .",
    "ohne-js": "bash tools/ohne-js-pruefen.sh",
    "ausrollen": "bash tools/ausrollen.sh"
  },
  "devDependencies": {
    "@sveltejs/adapter-node": "^5.2.0",
    "@sveltejs/kit": "^2.70.3",
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "svelte": "^5.57.0",
    "svelte-check": "^4.7.6",
    "typescript": "^6.0.3",
    "vite": "^8.3.0"
  }
}
