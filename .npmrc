# Keep the dependency graph explicit: no implicit hoisting into the root.
shamefully-hoist=false
# Fail early when a contributor runs an unsupported Node version.
engine-strict=true
# Workspace packages are always linked from the workspace, never from a registry.
link-workspace-packages=true
prefer-workspace-packages=true
